name: Hello world
on: push
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: Hello Step
        run: echo "hello build"

  test: 
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Hello Step
        run: echo "hello test"

  deploy: 
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Hello Step
        run: echo "hello deploy"
